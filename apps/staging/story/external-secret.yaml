apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: story-staging-secret
  namespace: nori-cloud
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: infisical-nori-cloud-staging
    kind: SecretStore
  target:
    name: story-staging-secret
    creationPolicy: Owner
  dataFrom:
    - find:
        name:
          regexp: STORY_*

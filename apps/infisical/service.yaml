apiVersion: v1
kind: Service
metadata:
  name: infisical
  namespace: nori-cloud
  labels:
    app: infisical
spec:
  type: LoadBalancer
  loadBalancerClass: tailscale
  selector:
    app: infisical
  ports:
    - port: 443
      targetPort: 8080
      name: https

apiVersion: v1
kind: Service
metadata:
  name: infisical
  namespace: nori-cloud
  labels:
    app: infisical
spec:
  type: ClusterIP
  selector:
    app: infisical
  ports:
    - port: 8080
      targetPort: 8080
      name: http

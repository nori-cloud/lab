apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: penpot-assets
  namespace: nori-cloud
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: freenas-iscsi
  resources:
    requests:
      storage: 5Gi

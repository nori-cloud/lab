apiVersion: v1
kind: Service
metadata:
  name: affine
  namespace: nori-cloud
  labels:
    app: affine
spec:
  type: ClusterIP
  selector:
    app: affine
  ports:
    - port: 3000
      targetPort: 3000
      name: http

apiVersion: v1
kind: ConfigMap
metadata:
  name: openclaw-config
  namespace: nori-cloud
  labels:
    app: openclaw
data:
  openclaw.json: |
    {
      "gateway": {
        "bind": "lan"
      }
    }

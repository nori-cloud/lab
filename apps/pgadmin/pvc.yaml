apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pgadmin-data
  namespace: nori-cloud
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: freenas-iscsi
  resources:
    requests:
      storage: 1Gi

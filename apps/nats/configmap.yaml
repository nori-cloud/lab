apiVersion: v1
kind: ConfigMap
metadata:
  name: nats-config
  namespace: nori-cloud
data:
  nats.conf: |
    server_name: $POD_NAME

    jetstream {
      store_dir: /data
    }

    http_port: 8222

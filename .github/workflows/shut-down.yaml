on:
  workflow_dispatch:
  schedule:
    - cron: "0 12 * * 5,6"

jobs:
  shutdown:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./cloud/aws
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - run: |
          echo "Goodbye, world!"

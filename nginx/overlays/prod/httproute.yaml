apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: nginx
spec:
  parentRefs:
    - name: nori-lab-gateway
      sectionName: http
  hostnames:
    - nginx.lab.norriswu.me
  rules:
    - backendRefs:
        - name: nginx
          port: 80

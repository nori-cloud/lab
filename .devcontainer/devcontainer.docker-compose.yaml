services:
  workspace:
    build:
      context: ..
      dockerfile: .devcontainer/Dockerfile
    command: sleep infinity
    volumes:
      - ../..:/workspace:cached
      - workspace_config:/root/

volumes:
  workspace_config:

apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: whoami
spec:
  parentRefs:
    - name: nori-lab-gateway
      sectionName: http
  hostnames:
    - whoami-lab.norriswu.me
  rules:
    - backendRefs:
        - name: whoami
          port: 80
